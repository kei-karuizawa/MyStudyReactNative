// 11-2：TypeScript 安装和项目配置

// 安装 TS：
// 1. `npm install --save-dev typescript`。TS 运行时没有类型的意思。
// 2. 生成 `tsconfig.json`：`tsc --init`
// 3. 安装类型申明（众多），这里一定要安装的两个：`npm i --save-dev @types/react @types/react-native`。
