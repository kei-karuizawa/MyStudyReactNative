// 4-6：使用nrm管理npm源

// node 官方源访问很慢，于是访问它的镜像源，于是就有很多这样的源需要管理，用 `nrm`。
// `nrm ls`：查看所有源。
// `nrm add <name> <source-link>`：添加一个源。
// nrm use <name>：使用（切换到）一个源。
// nrm test <name>：测试一个源，看源通不通。

// nrm 简化管理 npm 源的操作。

// 安装 nrm：`npm i -g nrm`，`-g` 表示全局安装。
