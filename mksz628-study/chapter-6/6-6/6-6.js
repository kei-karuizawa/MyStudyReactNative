// 6-6：函数式组件的优势和常用hook

// 函数式组件三种写法。
// 1. function 函数。
// 2. 箭头函数。
// 3. export default () => { return () }，不用写方法名直接导出。

// 常用 hook：
// 1. useState。一个 state 对应一个变化的值。
// 2. useEffect：副作用。
// 3. useRef：对象引用，可以通过 ref 去操作某个组件。可以通过命令的方式手动控制一个组件。
// 4. useWindowDimension：获取屏幕。
// 5. useColorScheme：主题。
