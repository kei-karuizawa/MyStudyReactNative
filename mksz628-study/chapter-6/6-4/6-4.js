// 6-4：import和export，三种导入导出类型

// 为什么可以 `import App from './App'` ？，因为我们在 `App.tsx` 里 `export default App` 了。
// 如果是 json 文件里的字段，可以不用 `export` 就可以被其他文件 `import` 使用。

// `default` 是默认导出对象，参照 `example1` 的示例。
// 导出函数还可以用 `example2` 示例下的方法。
