// 7-8：TouchableHighlight使用略显麻烦的点击组件

// 所有点击类时间和 `TouchableOpacity` 相同。

// `TouchableHighlight` 支持子节点，而且只能写一个子节点，而且必须写一个子节点，否则会报错。

// `TouchableHighlight` 必须覆写 `onPress` 回调函数，否则点击事件无效，点不动。

// `underlayColor`：设置按下去时高亮的颜色。
// 也有 `activeOpacity` 属性。
