// 7-7：TouchableOpacity最好用的点击组件

// `activeOpacity`：取值方位 0 ~ 1。按钮点击时，按钮不透明度变化，变化范围从 x ~ 1。

// 点击事件：
// 1. `onPress`。
// 2. `onLongPress`。
// 3. `delayLongPress`：指定长按延时多久算长按？单位是毫秒。

// 点击事件的起止：
// `onPressIn` 和 `onPressOut`。按下去触发 `onPressIn`，手指抬起来触发 `onPressOut`。
